services:
  - type: static
    name: mini-rrhh
    staticPublishPath: ./dist
    buildCommand: npm install && npm run build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
